# Relevant Files

- `backend/package.json` - Add `imapflow`, `mailparser`, `html-to-text` (+ `@types/mailparser`) dependencies.
- `db/migrations/0005_emails.sql` - New migration: `emails` table + `mailboxes.last_synced_at` column (PRD §3.1).
- `backend/test/migrate.test.ts` - Existing generic migration-runner test; update expected row count (4→5) and filename assertion for `0005_emails.sql`.
- `backend/test/migrate-cli.test.ts` - Existing CLI test with a hardcoded applied-migration count; update 4→5.
- `backend/test/emails-schema.test.ts` - New schema-shape test for migration 0005 (mirrors `mailboxes-schema.test.ts`'s pattern).
- `backend/src/mailboxes/connectors/types.ts` - Add `FetchedMessage`, `ListMessageIdsResult`, `FetchMessagesResult` types and extend `MailboxConnector` with `listMessageIds`/`fetchMessages` (PRD FR-1).
- `backend/src/mailboxes/connectors/pop3.ts` - Extend with `listMessageIds` (`LIST`/`UIDL`) and `fetchMessages` (`TOP`/`RETR` + `mailparser`) (PRD FR-3, FR-4, FR-8, FR-9).
- `backend/src/mailboxes/connectors/shared.ts` - Extend the shared TLS/line-reading plumbing if `pop3.ts`'s new multi-line/multi-step exchanges need it (e.g. reading a `RETR`/`TOP` response's dot-terminated multi-line body block).
- `backend/src/mailboxes/test/fixtures.ts` - Extend the fake POP3 TLS server to also handle `LIST`/`UIDL`/`TOP`/`RETR`, including a no-`UIDL` variant, an HTML-only fixture message, and an older-than-7-days fixture message.
- `backend/src/mailboxes/connectors/test/connectors.test.ts` - New/extended tests for POP3's `listMessageIds`/`fetchMessages` against the extended fake server (moved/co-located next to `connectors/`, mirrors Feature 3's `mailboxes/test/connectors.test.ts` for the new methods — confirm final path during implementation, may simply extend the existing `backend/src/mailboxes/test/connectors.test.ts`).
- `backend/src/mailboxes/connectors/imap-client.ts` - New: the small injectable IMAP client interface (`connect`/`getMailboxLock`/`search`/`fetch`/`logout`) plus the default factory wrapping the real `imapflow` package (PRD FR-2).
- `backend/src/mailboxes/connectors/imap.ts` - Rewritten: `testConnection`/`listMessageIds`/`fetchMessages` implemented against the injectable client interface, replacing Feature 3's hand-rolled socket code entirely.
- `backend/src/mailboxes/connectors/test/imap.test.ts` - New tests for `imap.ts` against a fake in-memory client (no real socket) — since-filtering, id list mapping, MIME hand-off, error mapping, `\Seen` flag mapping.
- `backend/src/mailboxes/connectors/index.ts` - No signature change expected, but confirm `getConnector` still returns the (rewritten) `imapConnector`/`pop3Connector` unchanged.
- `backend/src/sync/engine.ts` - New: `syncMailbox(db, vault, connector, mailboxId)` — the sync engine (PRD §3.3).
- `backend/src/sync/test/engine.test.ts` - Integration tests for the engine (embedded Postgres, fake connector) covering first-sync cap, incremental dedupe, idempotency/concurrency, error status transitions.
- `backend/src/mailboxes/dashboard.ts` - Update `loadAccounts` (real `unread` via query) and the dashboard handler (real `lastSync` via a new relative-time helper) (PRD FR-11, FR-12).
- `backend/src/mailboxes/test/dashboard.test.ts` - Extend existing dashboard tests for real `unread`/`lastSync` values.

---

# Tasks

- [x] 1.0 Add sync dependencies
  - [x] 1.1 Run `pnpm --filter @pigeon/backend add imapflow mailparser html-to-text` and `pnpm --filter @pigeon/backend add -D @types/mailparser` with the bash tool (not TDD — a dependency install). Confirm `backend/package.json` picks up all four and `pnpm-lock.yaml` updates. **Confirmed:** all four present in `backend/package.json` (`imapflow ^1.4.3`, `mailparser ^3.9.12`, `html-to-text ^10.0.0`, `@types/mailparser ^3.4.6`).
  - [x] 1.2 CHECK PHASE: Run `pnpm check` with the bash tool — confirm the new deps don't break typecheck/lint on their own (no code uses them yet). **Confirmed:** lint clean, all 3 workspaces typecheck clean.

- [x] 2.0 Database: `emails` table + `mailboxes.last_synced_at` migration
  - [x] 2.1 RED: Write failing tests with the write-test agent:
    - `backend/test/emails-schema.test.ts` (new, mirrors `mailboxes-schema.test.ts`): after `runMigrations`, assert `to_regclass('public.emails')` is not null; assert each column (`id`, `mailbox_id`, `provider_uid`, `seen`, `from_name`, `from_address`, `subject`, `body`, `received_at`, `created_at`) exists with the right type/nullability via `information_schema.columns`; assert the `UNIQUE (mailbox_id, provider_uid)` constraint exists (query `information_schema.table_constraints`/`pg_constraint`); assert `mailboxes.last_synced_at` exists, nullable, `TIMESTAMPTZ`.
    - Update `backend/test/migrate.test.ts`'s row-count/filename assertions (4→5, add `{ id: 5, filename: "0005_emails.sql" }`) and the idempotency test's expected count (4→5).
    - Update `backend/test/migrate-cli.test.ts`'s hardcoded applied-migration count (4→5).
  - [x] 2.2 CONFIRM RED: Run `pnpm exec vitest run backend/test/emails-schema.test.ts backend/test/migrate.test.ts backend/test/migrate-cli.test.ts` with the bash tool — verify failures (missing table, wrong row counts). **Confirmed:** 10/13 failed as expected (`to_regclass` null, missing columns, `relation "emails" does not exist`, counts 4≠5). **Note:** the task list's original command (`pnpm --filter @pigeon/backend test ...`) doesn't exist — there is no per-workspace `test` script; corrected to `pnpm exec vitest run <paths>` per `docs/COMMANDS.md`.
  - [x] 2.3 GREEN: Implement `db/migrations/0005_emails.sql` with the write-code agent per PRD §3.1: `emails` table (all columns, `UNIQUE (mailbox_id, provider_uid)`, index on `mailbox_id`, `ON DELETE CASCADE` from `mailboxes`), plus `ALTER TABLE mailboxes ADD COLUMN last_synced_at TIMESTAMPTZ NULL`.
  - [x] 2.4 CONFIRM GREEN: Run the same test command — verify all pass. **Confirmed:** 13/13 passed.
  - [x] 2.5 REFACTOR: Migration file reviewed against `0004_mailboxes.sql`'s style — already clean and consistent; no refactor needed.
  - [x] 2.6 CHECK PHASE: Run `pnpm check` with the bash tool. **Confirmed:** lint clean, all 3 workspaces typecheck clean.

- [x] 3.0 POP3 connector: `listMessageIds` + `fetchMessages`
  - [x] 3.1 RED: Write-test agent extends `backend/src/mailboxes/test/fixtures.ts` and writes new tests:
    - Extend the fake POP3 server to handle `LIST` (returns scripted message numbers/sizes), `UIDL` (returns scripted stable ids per message — and a separate fixture variant that responds `-ERR` to bare `UIDL` to simulate no support), `TOP <n> 0` (returns headers-only for message `n`), and `RETR <n>` (returns the full scripted message, headers + body, dot-terminated per POP3 framing).
    - Fixture messages: at least one plain-text message, one HTML-only message (no `text/plain` part), one with a `Date` header older than 7 days, and the rest within the last 7 days.
    - New/extended `backend/src/mailboxes/test/connectors.test.ts` cases: `listMessageIds` returns the expected UIDL-derived ids; against the no-`UIDL` fixture, resolves `{ ok: false, reason: "uidl_not_supported" }`; `fetchMessages` for a given id set returns correctly parsed `FetchedMessage[]` (from/subject/body/receivedAt/seen); the HTML-only message's `body` is non-empty derived plain text; a first-sync-style call (headers peeked via `TOP`, then `RETR` only for in-window ids) only fetches full bodies for messages within the last 7 days, verified by asserting the returned set excludes the old fixture message. **Design gap found + resolved:** `fetchMessages` needed a third `opts?: { since?: Date }` param (mirroring `listMessageIds`) for POP3 to know when to apply the 7-day peek-and-filter — PRD FR-1 amended to match before writing the filter test.
  - [x] 3.2 CONFIRM RED: Run `pnpm exec vitest run backend/src/mailboxes/test/connectors.test.ts` with the bash tool — verify it fails (methods don't exist yet on `pop3Connector`). **Confirmed:** 5/15 failed (`connector.listMessageIds/fetchMessages is not a function`); the 10 pre-existing `testConnection` tests (both protocols) still passed.
  - [x] 3.3 GREEN: Write-code agent implements `listMessageIds`/`fetchMessages` on `backend/src/mailboxes/connectors/pop3.ts` per PRD FR-3/FR-4/FR-8/FR-9 (extending `shared.ts`'s TLS/line-reading plumbing with a new `readDotTerminatedBlock` helper, generalizing `testTlsConnection` to be generic over the result shape), using `mailparser`/`html-to-text` for body extraction. Updated `backend/src/mailboxes/connectors/types.ts` with the new shared types and extended `MailboxConnector`.
  - [x] 3.4 CONFIRM GREEN: Run the same test command — verify all pass. **Confirmed:** 15/15 passed.
  - [x] 3.5 REFACTOR: Implementation already clean on review (shared login flow factored into `withPop3Login`, block-reading factored into `shared.ts`'s `readDotTerminatedBlock`); no further refactor needed.
  - [x] 3.6 CHECK PHASE: Run `pnpm check` with the bash tool. **Found + fixed 3 regressions** from extending the shared `MailboxConnector` interface: (1) `imap.ts`'s `imapConnector` now missing the two new methods — added provisional stub implementations (`{ ok: false, reason: "not implemented" }`) explicitly marked as replaced by task 4.0's rewrite; (2) `html-to-text` ships no type declarations and `@types/html-to-text` only covers up to v9 (mismatched against installed v10) — added a minimal local ambient declaration `backend/src/mailboxes/connectors/html-to-text.d.ts` for just the `convert` export actually used; (3) `backend/src/mailboxes/test/routes.test.ts`'s two fake-connector object literals (Feature 3, unrelated to sync) needed the same two stub methods to satisfy the type. Re-ran `pnpm check` (clean) and `pnpm exec vitest run` on both `routes.test.ts` and `connectors.test.ts` (23/23 passed, no behavior regression).

- [x] 4.0 IMAP connector: rewrite on `imapflow` behind an injectable client interface
  - [x] 4.1 RED: Write-test agent writes:
    - `backend/src/mailboxes/connectors/imap-client.ts`'s interface is consumed (not yet implemented) by new tests in `backend/src/mailboxes/test/imap.test.ts`: a fake in-memory client (implementing `connect`/`getMailboxLock`/`search`/`fetch`/`logout`/`close`) is injected into `imap.ts`'s connector functions. Cover: `testConnection` resolves `{ ok: true }` on a successful fake `connect()` (and calls `logout()`), `{ ok: false, reason: "authentication failed" }` when the fake rejects with `authenticationFailed: true`, `{ ok: false, reason: "could not reach ..." }` on a plain network-error rejection; `listMessageIds` calls the fake's `search` with `{ all: true }` (no `opts`) or `{ since }` (with `opts.since`), always paired with `{ uid: true }`, mapping returned UIDs to strings; `fetchMessages` maps each yielded item into a `FetchedMessage` (MIME via `mailparser`, `seen` reflecting `\Seen` in `flags`); an HTML-only fixture message produces non-empty, tag-free plain text via `html-to-text`; all three methods resolve `{ ok: false }` (never throw) when the fake rejects.
    - Also modified `backend/src/mailboxes/test/connectors.test.ts`: removed IMAP from the shared `describe.each(protocols)` `testConnection` block (simplified to a plain POP3-only `describe`), since IMAP's `testConnection` coverage relocated to the new fake-client-based tests above — POP3's cases and the newer `listMessageIds`/`fetchMessages` block were left untouched.
  - [x] 4.2 CONFIRM RED: Run `pnpm exec vitest run backend/src/mailboxes/test/imap.test.ts backend/src/mailboxes/test/connectors.test.ts` with the bash tool — verify it fails. **Confirmed:** 9/9 new IMAP tests failed (`createImapConnector is not a function`); the restructured POP3-only `connectors.test.ts` still passed 10/10 (no regression from the relocation).
  - [x] 4.3 GREEN: Write-code agent implements:
    - `backend/src/mailboxes/connectors/imap-client.ts` — the injectable interface + `defaultImapFlowFactory` wrapping the real `imapflow` package.
    - `backend/src/mailboxes/connectors/imap.ts` — rewritten `testConnection`/`listMessageIds`/`fetchMessages` against the interface, removing Feature 3's hand-rolled socket/tag-matching code entirely.
  - [x] 4.4 CONFIRM GREEN: Run the same test command — verify all pass. **Confirmed:** 19/19 passed (9 IMAP + 10 POP3).
  - [x] 4.5 REFACTOR: Found and fixed a real inconsistency: `listMessageIds`/`fetchMessages` didn't guard `logout()`/`close()` the way `testConnection` did — a `logout()` throw after a successful search/fetch incorrectly discarded the already-fetched result as `{ ok: false }`, and `close()` was unreachable in that path (connection leak). Write-code agent hardened both to match `testConnection`'s best-effort-logout + always-`close()`-in-`finally` pattern. CONFIRM GREEN: re-ran the same command, still 19/19.
  - [x] 4.6 CHECK PHASE: Run `pnpm check` with the bash tool. **Confirmed:** lint clean, all 3 workspaces typecheck clean. Also ran the full `backend/src/mailboxes` test directory (30/30 passed) as an extra safety check given the scope of the connector rewrite.

- [x] 5.0 Sync engine (`backend/src/sync/`)
  - [x] 5.1 RED: Write-test agent writes `backend/src/sync/test/engine.test.ts` (integration; `withTestDb`, migrations applied, a mailbox row seeded directly, a fake `MailboxConnector` injected — no real network). Covers first-sync `since` bound (~7 days), incremental sync omitting `since`, diffing against already-stored `provider_uid`s, zero-new-rows re-runs, the `ON CONFLICT DO NOTHING` idempotency backstop, and connector-failure `status='error'`/`last_synced_at`-untouched. **Design gap found + resolved:** the PRD named `Promise<SyncResult>` without ever defining `SyncResult` — added `{ ok: true; inserted: number } | { ok: false; reason: string }` to the PRD (§3.3 FR-6) before the GREEN step.
  - [x] 5.2 CONFIRM RED: Run `pnpm exec vitest run backend/src/sync/test/engine.test.ts` with the bash tool — verify it fails (module doesn't exist). **Confirmed:** "Cannot find module '../engine'".
  - [x] 5.3 GREEN: Write-code agent implements `backend/src/sync/engine.ts` — `syncMailbox(db, vault, connector, mailboxId)` per PRD §3.3 FR-6/FR-7/FR-8: load mailbox + decrypt credential via `vault.open`, set `status='syncing'`, `listMessageIds` (with/without `since`), diff against `SELECT provider_uid FROM emails WHERE mailbox_id = $1`, `fetchMessages` for the new subset only (threading `since` through on a first sync too, so POP3's peek-and-filter engages), `INSERT ... ON CONFLICT (mailbox_id, provider_uid) DO NOTHING RETURNING id` (counting actual inserts), then `status='connected'`/`last_synced_at=now()` on success or `status='error'` (leaving `last_synced_at`) on any connector failure.
  - [x] 5.4 CONFIRM GREEN: Run the same test command — verify all pass. **Confirmed:** 6/6 passed.
  - [x] 5.5 REFACTOR: Implementation reviewed — clean module-doc comment, clear step-by-step flow, no duplication; no refactor needed.
  - [x] 5.6 CHECK PHASE: Run `pnpm check` with the bash tool. **Confirmed:** lint clean, all 3 workspaces typecheck clean.

- [x] 6.0 Dashboard: real `unread` and `lastSync`
  - [x] 6.1 RED: Write-test agent extends `backend/src/mailboxes/test/dashboard.test.ts`: seed one IMAP mailbox with a mix of `seen=true`/`seen=false` `emails` rows and assert `accounts[].unread` matches the `seen=false` count (2); seed a second, POP3-protocol mailbox with unseen-flagged rows and assert its `unread` is always `0`; seed `mailboxes.last_synced_at` on two mailboxes with different timestamps and assert `lastSync` matches `/ago$/` and is not `"Never"` (the existing first test already covers the `"Never"` case for mailboxes that never synced — not duplicated).
  - [x] 6.2 CONFIRM RED: Run `pnpm exec vitest run backend/src/mailboxes/test/dashboard.test.ts` with the bash tool — verify the new assertions fail. **Confirmed:** 2/6 failed (`unread` 0≠2, `lastSync` "Never"≠/ago$/); the POP3-always-0 case passed trivially pre-GREEN (everything was hardcoded 0) and was re-verified post-GREEN too.
  - [x] 6.3 GREEN: Write-code agent updates `backend/src/mailboxes/dashboard.ts`: `loadAccounts` computes `unread` via one grouped query (`COUNT(*) FILTER (WHERE seen = false) ... GROUP BY mailbox_id`, `WHERE mailbox_id = ANY(${ids}::uuid[])`) for IMAP mailboxes only, POP3 hardcoded `0` without running the query; a new small local `formatRelativeTime` helper formats `MAX(mailboxes.last_synced_at)` into `lastSync`, `"Never"` when null.
  - [x] 6.4 CONFIRM GREEN: Run the same test command — verify all pass. **Confirmed:** 6/6 passed, including the previously-unverified `ANY(...)::uuid[]` array-parameter query pattern (new to this codebase, worked correctly against postgres.js).
  - [x] 6.5 REFACTOR: Implementation reviewed — relative-time helper is small/local as intended, grouped-query approach is a clean, minimal diff; no refactor needed.
  - [x] 6.6 CHECK PHASE: Run `pnpm check` with the bash tool. **Confirmed:** lint clean, all 3 workspaces typecheck clean.

- [x] 7.0 Final: full suite + manual sanity check
  - [x] 7.1 Run `pnpm check:all` with the bash tool across the whole repo — all must pass (lint, typecheck all 3 workspaces, full test suite, frontend build). **Confirmed:** `pnpm check:all` — 26/26 test files, 166/166 tests passed, lint clean, all 3 workspaces typecheck clean. `pnpm --filter @pigeon/frontend build` (not part of `check:all`, run separately per the task) — 9/9 pages built, clean. No regressions found; nothing needed fixing forward.
  - [ ] 7.2 Manual verification instructions for the user (not automated — no HTTP route exists yet to click through in this feature): with a throwaway real (or locally-run) IMAP/POP3 mailbox, seed a `mailboxes` row (e.g. via a short one-off script or the existing connect flow) and call `syncMailbox()` directly (e.g. via a scratch script using `tsx`) to confirm it fetches real recent mail into `emails` end-to-end, then call it again to confirm the second run inserts nothing new.

- [ ] Commit message (not committed — user manages VCS): `feat(sync): add incremental mailbox sync engine with IMAP/POP3 fetch, dedupe, and 7-day first-sync cap`
